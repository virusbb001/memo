
 portsの使い方
  cd /usr/ports
  目的のポートへ
  make install clean

インストール先
 /usr/local

  これ以降あるポートをいじる際はそのディレクトリに移動してやること
 ports検索
  
ports検索
 http://www.freebsd.org/ja/ports/searching.html
 ここから調べるか

 cd /usr/ports
 make search name=検索

依存するファイルをダウンロードする
 make fetch-recursive

portの設定
 make config
